plugins:
  - jekyll-redirect-from
  - jekyll-org
  - jekyll-git_metadata

collections:
  links:
    output: true
  thoughts:
    output: true
  terms:
    output: true

defaults:
  - scope:
      type: "thoughts"
    values:
      layout: "thought"
      subtitle: "a thought."
      subtitle_link: "/thoughts"
  - scope:
      type: "terms"
    values:
      layout: "term"
      subtitle: "a term."
      subtitle_link: "/terms"
org:
  liquid: true
